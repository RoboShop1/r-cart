name: two Workflow

on:
  workflow_run:
    workflows: ["one workflow"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: print
        run: |
          echo "This is ${GITHUB_EVENT_WORKFLOW_RUN.CONCLUSION}"
          echo "same  ${GITHUB_EVENT_NAME}"
          echo "RUN ${GITHUB_RUN_NUMBER}"
          
